import React from "react";
import {SafeAreaView, ImageBackground, Text, View} from "react-native";

import HandsBackground from "../components/HandsBackground";

const PrimeScreen = () => {
    return <SafeAreaView style={{flex: 1}}>
        <HandsBackground style={{flex: 1}}>
            <Text style={{fontSize: 60, color: 'white'}}>
                MUIEEEEEEE
            </Text>
        </HandsBackground>
    </SafeAreaView>
} 

export default PrimeScreen;

